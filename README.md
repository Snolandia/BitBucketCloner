# BitBucketCloner
Easy way to clone all BitBucket repositories of a user to a folder designated in the code. A consumer key/secret will be needed, instructions in code on how to make one. Just fill in the first 3 variables with the correct information, then run. Python required.
